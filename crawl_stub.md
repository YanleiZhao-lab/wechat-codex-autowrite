# 爬取阶段占位说明

当前项目先把“固定套路”定下来，但还没有对具体来源做正式爬虫适配。

推荐接入方式：

1. **手动 URL 列表抓取**
   - 输入一个 URL 列表
   - 用 OpenClaw 的 `web_fetch` / browser 能力抓正文
   - 保存到 `collected/<topic>/raw/`

2. **关键词搜索抓取**
   - 先按关键词搜文章
   - 再抓命中的页面
   - 保存原链接和抓取时间

3. **公众号类来源**
   - 很多页面反爬较重
   - 不建议一开始就做强耦合爬虫
   - 先支持“给 URL -> 抓正文 -> 失败则人工补素材”

建议先把抓取接口做成：
- 输入：topic / urls / keywords
- 输出：raw 素材目录

后面再按来源逐个适配。
